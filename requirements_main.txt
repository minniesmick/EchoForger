# ──────────────────────────────────────────────────────────────────────────────
# TTS Studio — Bağımlılıklar
#
# KURULUM SIRASI ÖNEMLİDİR:
#
# 1. Önce CUDA destekli PyTorch (requirements.txt'ten ÖNCE çalıştırın):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# 2. Ardından bu dosyayı çalıştırın:
#    pip install -r requirements.txt
#
# 3. pydub ses birleştirme için ffmpeg gereklidir:
#    https://ffmpeg.org/download.html → Windows için binary indirin, PATH'e ekleyin.
#
# 4. Hume AI TADA kullanıyorsanız API anahtarı ortam değişkeni set edin:
#    set HUME_API_KEY=your_api_key_here
# ──────────────────────────────────────────────────────────────────────────────


# ── Kullanıcı Arayüzü ────────────────────────────────────────────────────────
PyQt6>=6.6.0


# ── Motor 1: Coqui XTTSv2 ────────────────────────────────────────────────────
# NOT: torch ayrıca pip install edilmeli (yukarıdaki CUDA komutuyla).
coqui-tts>=0.24.0

# ── Motor 2: Fish Speech V1.5 ────────────────────────────────────────────────
# Lokal kurulum; model dosyaları D:\Ses_Modelleri\fish-speech-1.5\ altına alınmalı.
# Motor 2: Fish Speech V1.5
# PyPI'da bulunmadığı için doğrudan GitHub'dan çekiyoruz:
git+https://github.com/fishaudio/fish-speech.git


# ── Motor 3: Hume AI TADA ─────────────────────────────────────────────────────
# Bulut tabanlı; HUME_API_KEY ortam değişkeni zorunludur.
# Hesap: https://platform.hume.ai
hume-tada>=0.1.0



# ── Opsiyonel: Geliştirme Araçları ───────────────────────────────────────────
# Aşağıdakiler projeyle doğrudan ilgili değildir; geliştirme ortamı için eklenebilir.
# mypy>=1.8.0          # Tip denetimi
# ruff>=0.3.0          # Linter & formatter