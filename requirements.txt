# ══════════════════════════════════════════════════════════
# EchoForge — Requirements
# ══════════════════════════════════════════════════════════
#
# KURULUM SIRASI ÖNEMLİ — adım adım uygula:
#
# Adım 1 — PyTorch (CUDA 11.8, .venv için):
#   pip install torch==2.0.1+cu118 torchaudio==2.0.2+cu118 \
#     --index-url https://download.pytorch.org/whl/cu118
#
# Adım 2 — Kalan paketler:
#   pip install -r requirements.txt
#
# NOT: Fish Speech kaldırıldı. .venv_fish artık kullanılmıyor.
# ══════════════════════════════════════════════════════════

# ── UI ────────────────────────────────────────────────────
PyQt6>=6.5.0

# ── STT — Transkripsiyon ──────────────────────────────────
faster-whisper>=1.0.0

# ── TTS — Ses Üretimi ─────────────────────────────────────
# Coqui TTS (XTTSv2)
TTS>=0.22.0

# cuDNN uyumsuzluk sorunu yaşarsan:
# pip install nvidia-cudnn-cu11==8.6.0.163
nvidia-cudnn-cu11==8.6.0.163

# ── Ses işleme ────────────────────────────────────────────
pydub>=0.25.1
soundfile>=0.12.1

# ── Görselleştirme ────────────────────────────────────────
pyqtgraph>=0.13.3
numpy>=1.24.0

# ── Hume AI TADA (opsiyonel, bulut TTS) ──────────────────
# Kullanmayacaksan bu satırı yorum yap:
# hume-tada

# ── Ollama (TTT modu için — ileride) ─────────────────────
# pip install ollama   (hazır olunca eklenecek)
